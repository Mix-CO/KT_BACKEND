name: Backend CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  backend-ci:
    uses: ./.github/workflows/reusable-backend-ci.yml
    secrets: inherit