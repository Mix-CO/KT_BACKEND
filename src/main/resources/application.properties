spring.application.name=backend

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.ovzocperakrbvcyoaqqs}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:507202kcktm}

# OAuth2 - Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:test-dummy-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:test-dummy-client-secret}
spring.security.oauth2.client.registration.google.scope=email,profile

spring.jpa.hibernate.ddl-auto=update
server.port=${PORT:8080}
app.frontend-url=http://localhost:5173

groq.api.key=${GROQ_API_KEY:test-dummy-key-not-real}